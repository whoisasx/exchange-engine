# Engine Context Bundle

Start with `engine-contract.md` for the stream protocol. Use
`scripts/engine-smoke.sh --help` for local smoke verification.

Use `examples/` as the JSON conformance fixtures for C++ serializers and
parsers.
